<article class="post">
    <div>
        <h1>{{post.title}}</h1>
    </div>
    <div>
        {{post.created_at}}

        <a href="/post/{{post.id}}">{{poast.comments.length}} comment(s)</a>
    </div>
</article>