<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Total Tech News</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
    <h1 class="display-4"><a href="/">Welcome to Total-Tech-News</a></h1>
    <h2 class="lead">Your one stop tech spot!</h2>
    <hr class="my-4">
    <nav>
        <div class="justify-content-center">
        <a type="button" class="btn btn-secondary btn-sm" href="/">Home</a>
        {{#if loggedIn}}
        <a type="button" class="btn btn-secondary btn-sm" href="/dashboard">Dashboard</a>
        <button class="btn btn-secondary btn-sm" id="logout">Logout</button>
        {{else}}
        <a type="button" class="btn btn-secondary btn-sm" href="/login">Login</a>
        {{/if}}
        </div>
      </nav>
    {{{ body }}}

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}    
</body>

</html>